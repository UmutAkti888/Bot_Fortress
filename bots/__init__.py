# This file marks the bots/ folder as a Python package.
# Each bot lives in its own module here (arxiv_bot.py, future_bot.py, etc.)
# BotHub (bothub/app.py) imports from this package to power the web dashboard.
